/**
 * M02 route readability — subtle entrance pavers only (no debug rings).
 *
 * Route identity is conveyed by facility facades, signage, and corridor polish.
 * This component is intentionally empty; entrance pavers live in M02CorridorPolish.
 */
export function RouteMarkers() {
  return null;
}
